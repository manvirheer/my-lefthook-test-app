# lefthook.yml
colors:
  red:   '#E6D8FF'
  green: '#DFF8F4'
output: [summary, failure, execution_out]

commit-msg:
  parallel: true
  commands:
    validate-message:
      run: bash scripts/validate-message.sh   
    no-merge-commit:
      run: bash scripts/no-merge-commit.sh
      fail_text: |
        ⛔  Merge commit detected. Please rebase.

pre-push: 
  commands:
    install:
      priority: 10
      run: yarn 
    build:
      priority: 20
      run: yarn build