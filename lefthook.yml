# lefthook.yml
colors:
  red:   '#E6D8FF'
  green: '#DFF8F4'
output: [summary, failure, execution_out]

commit-msg:
  scripts:
    "no-merge-commits.sh":
      runner: bash
      use_stdin: true

pre-push: 
  scripts:
    "hello-world.sh": 
      runner: bash

  commands:
    install:
      priority: 10
      run: yarn 
    build:
      priority: 20
      run: yarn build