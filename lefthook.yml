# lefthook.yml
colors:
  red:   '#E6D8FF'
  green: '#DFF8F4'
output: [summary, failure, execution_out]

commit-msg:
  parallel: true
  scripts:
    validate-message.sh:
      runner: bash   
    no-merge-commit.sh:
      runner: bash 
      fail_text: |
        ⛔  Merge commit detected. Please rebase.

pre-push: 
  commands:
    # Print Start message
    start:
      priority: 8
      run: echo "Starting pre-push hook..."
    install:
      priority: 10
      run: yarn 
    build:
      priority: 20
      run: yarn build